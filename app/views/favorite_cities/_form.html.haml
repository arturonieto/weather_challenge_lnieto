= form_for @favorite_city do |f|
  - if @favorite_city.errors.any?
    #error_explanation
      %h2= "#{pluralize(@favorite_city.errors.count, "error")} prohibited this favorite_city from being saved:"
      %ul
        - @favorite_city.errors.full_messages.each do |message|
          %li= message

  .actions
    = f.submit 'Save'
